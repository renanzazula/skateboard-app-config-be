package com.skateboard.appconfig.domain.model;

/**
 * Horizontal alignment of a {@link CampaignScreen}'s title and description
 * (spec §7). The client maps these to theme typography rather than arbitrary
 * styling.
 */
public enum CampaignTextAlignment {
    LEFT,
    CENTER,
    RIGHT
}
